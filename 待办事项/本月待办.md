# 本月待办事项 (2026年6月)

## 高优先级任务

### 📝 综述撰写 [due:: 2026-06-25] #高优先级

> [!example] 进度
> ````dataviewjs
> const page = dv.current();
> const tasks = page.file.tasks;
> const subtasks = tasks.filter(t => t.text.includes("收集文献") || t.text.includes("整理研究") || t.text.includes("撰写初稿") || t.text.includes("修改完善"));
> const done = subtasks.filter(t => t.completed).length;
> const total = subtasks.length;
> const pct = total > 0 ? Math.round(done / total * 100) : 0;
> const bar = "🟩".repeat(Math.round(pct / 20)) + "⬜".repeat(5 - Math.round(pct / 20));
> dv.paragraph(`**${done}/${total}** (${pct}%) ${bar}`);
> ````

- [ ] 综述撰写（主任务）
  - [ ] 收集文献资料
  - [ ] 整理研究框架
  - [ ] 撰写初稿
  - [ ] 修改完善

### 📄 期刊审稿 [due:: 2026-06-22] #高优先级

> [!example] 进度
> ````dataviewjs
> const page = dv.current();
> const tasks = page.file.tasks;
> const subtasks = tasks.filter(t => t.text.includes("通读全文") || t.text.includes("检查研究") || t.text.includes("评估实验") || t.text.includes("撰写审稿") || t.text.includes("提交审稿"));
> const done = subtasks.filter(t => t.completed).length;
> const total = subtasks.length;
> const pct = total > 0 ? Math.round(done / total * 100) : 0;
> const bar = "🟩".repeat(Math.round(pct / 20)) + "⬜".repeat(5 - Math.round(pct / 20));
> dv.paragraph(`**${done}/${total}** (${pct}%) ${bar}`);
> ````

- [x] 期刊审稿（主任务）
  - [x] 通读全文
  - [x] 检查研究方法
  - [x] 评估实验结果
  - [x] 撰写审稿意见
  - [x] 提交审稿报告

---

## 任务详情

### 综述撰写
- **截止日期**: 2026-06-25
- **提醒日期**: 2026-06-22 (截止前3天)
- **状态**: 进行中
- **优先级**: 高

### 期刊审稿
- **截止日期**: 2026-06-22
- **状态**: 已完成
- **优先级**: 高
- **笔记**: [[2026-6/期刊审稿-TAML|查看审稿笔记]]

---

## 即将到期任务 (3天内)

> ⚠️ 以下任务将在3天内到期，请优先处理！

```dataview
TASK
WHERE !completed
AND due
AND due <= date(today) + dur(3 days)
AND due >= date(today)
SORT due ASC
```

---

## 本月所有任务

### 📝 综述撰写 [due:: 2026-06-25] #高优先级

> [!example] 进度
> ````dataviewjs
> const page = dv.current();
> const tasks = page.file.tasks;
> const subtasks = tasks.filter(t => t.text.includes("收集文献") || t.text.includes("整理研究") || t.text.includes("撰写初稿") || t.text.includes("修改完善"));
> const done = subtasks.filter(t => t.completed).length;
> const total = subtasks.length;
> const pct = total > 0 ? Math.round(done / total * 100) : 0;
> const bar = "🟩".repeat(Math.round(pct / 20)) + "⬜".repeat(5 - Math.round(pct / 20));
> dv.paragraph(`**${done}/${total}** (${pct}%) ${bar}`);
> ````

- [ ] 综述撰写（主任务）
  - [ ] 收集文献资料
  - [ ] 整理研究框架
  - [ ] 撰写初稿
  - [ ] 修改完善

### 📄 期刊审稿 [due:: 2026-06-22] #高优先级

> [!example] 进度
> ````dataviewjs
> const page = dv.current();
> const tasks = page.file.tasks;
> const subtasks = tasks.filter(t => t.text.includes("通读全文") || t.text.includes("检查研究") || t.text.includes("评估实验") || t.text.includes("撰写审稿") || t.text.includes("提交审稿"));
> const done = subtasks.filter(t => t.completed).length;
> const total = subtasks.length;
> const pct = total > 0 ? Math.round(done / total * 100) : 0;
> const bar = "🟩".repeat(Math.round(pct / 20)) + "⬜".repeat(5 - Math.round(pct / 20));
> dv.paragraph(`**${done}/${total}** (${pct}%) ${bar}`);
> ````

- [x] 期刊审稿（主任务）
  - [x] 通读全文
  - [x] 检查研究方法
  - [x] 评估实验结果
  - [x] 撰写审稿意见
  - [x] 提交审稿报告
